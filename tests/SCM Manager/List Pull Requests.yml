info:
  name: List Pull Requests
  type: http
  seq: 7

http:
  method: GET
  url: "{{HOST}}/scm/api/v2/pull-requests/NPPS_EPI-DL/EPI-DL"
  headers:
    - name: Content-Type
      value: application/vnd.scmm-pullRequest+json;v=2
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
