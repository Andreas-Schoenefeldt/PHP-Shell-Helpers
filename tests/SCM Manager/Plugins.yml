info:
  name: Plugins
  type: http
  seq: 3

http:
  method: GET
  url: "{{HOST}}/scm/api/v2/ui/plugins"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
