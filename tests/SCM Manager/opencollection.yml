opencollection: 1.0.0

info:
  name: SCM Manager
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: basic
    username: "{{USER}}"
    password: "{{PASSWORD}}"
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
