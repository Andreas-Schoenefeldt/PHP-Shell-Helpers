info:
  name: Repositories
  type: http
  seq: 4

http:
  method: GET
  url: "{{HOST}}/scm/api/v2/repositories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
