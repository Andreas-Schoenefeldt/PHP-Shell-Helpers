info:
  name: List Projects
  type: http
  seq: 1

http:
  method: GET
  url: "{{HOST}}/api/v4/projects"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
